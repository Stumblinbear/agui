# Bundled test fonts

`Cantarell-Regular.ttf` — Cantarell, by Dave Crossland and the GNOME project, licensed
under the SIL Open Font License, Version 1.1 (<https://openfontlicense.org>). Bundled so
text golden tests shape against a fixed font rather than a host system font, keeping the
rendered output deterministic across machines.
